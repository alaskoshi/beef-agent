import {chromium} from 'playwright';const b=await chromium.launch({channel:'chrome',headless:true});const p=await b.newPage({viewport:{width:1440,height:900}});await p.goto('http://127.0.0.1:4173');console.log(await p.locator('svg.mech').first().evaluate(el=>({rect:el.getBoundingClientRect().toJSON(),height:getComputedStyle(el).height,width:getComputedStyle(el).width,viewBox:el.getAttribute('viewBox'),body:el.querySelector('.body').getBoundingClientRect().toJSON()})));console.log(await p.locator('.ring').evaluate(el=>({rect:el.getBoundingClientRect().toJSON(),height:getComputedStyle(el).height})));await p.screenshot({path:'output/playwright/production-initial.png'});await b.close();
